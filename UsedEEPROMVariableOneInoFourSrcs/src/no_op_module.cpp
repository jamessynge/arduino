// This compilation unit helps confirm that we're compiling the correct code
// because the compiler emits a warning about EEPROM being unused if EEPROM
// is declared static.

#include <Arduino.h>
#include <EEPROM.h>
